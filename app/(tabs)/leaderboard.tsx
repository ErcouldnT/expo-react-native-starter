import { Text, SafeAreaView } from "react-native";

export default function Board() {
  return (
    <SafeAreaView className="flex-1 justify-center items-center">
      <Text>Liderlik Tablosu</Text>
    </SafeAreaView>
  );
}
